export function min(first, second) {
  if (first < second) {
    return first;
  } else {
    return second;
  }
}

export function max(first, second) {
  if (first > second) {
    return first;
  } else {
    return second;
  }
}
